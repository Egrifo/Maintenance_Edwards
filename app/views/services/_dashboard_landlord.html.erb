          <div class="row">
            <!-- Content Column -->
            <div class="col-lg-12 mb-4">
              <!-- Project Card Example -->
              <div class="card shadow mb-6">
                <div class="card-header py-3">
                  <h6 class="m-0 font-weight-bold text-primary">Available Services</h6>
                </div>
                <%= @contract %>
              <table class="table table-bordered table-hover" id="dataTable" width="100%" cellspacing="0">
                <thead>
                  <tr>
                    <th>Name</th>
                    <th>Price</th>
                    <th>Duration</th>
                    <th>Margin</th>                               
                    <th colspan="3"><%= link_to fa_icon("plus", text: "Create"), new_service_path%></th>
                  </tr>
                  </thead>
                  <tbody>
                    <% @services.each do |f| %>
                      <tr>
                        <td><%= f.name %></td>
                        <td><%= f.price %>€</td>
                        <td><%= f.duration %></td>
                        <td><%= ((((f.price-f.cost)/(f.price)).round(2))*100)%>%</td>
                        <td><%= link_to fa_icon("id-badge", text: "Details"), f %></td>
                        <td><%= link_to fa_icon('pencil-square-o', text: "Edit"), edit_service_path(f) %></td>
                        <td><%= link_to fa_icon('trash', text: "Delete"), f, method: :delete, data: { confirm: 'Are you sure?' } %></td> 
                      </tr>
                    <% end %>
                  </tbody>
                </table>
                </div>
              </div>
            </div> 